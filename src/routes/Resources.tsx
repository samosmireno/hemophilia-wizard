/**
 * `/resources` — curated Resources panel, in-flow walkthrough step (content is
 * issue 12). Distinct from the off-line `/references` bibliography page.
 */
export default function Resources() {
  return (
    <section aria-labelledby="resources-heading">
      <h1 id="resources-heading">Resources</h1>
      <p>Resources placeholder.</p>
    </section>
  );
}
