/**
 * `/explore` — explore-the-agents view (build is issue 09). Placeholder stub.
 */
export default function Explore() {
  return (
    <section aria-labelledby="explore-heading">
      <h1 id="explore-heading">Explore</h1>
      <p>Explore placeholder.</p>
    </section>
  );
}
