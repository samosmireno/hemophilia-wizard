/**
 * `/` — standalone landing page (design/build is issue 17). Placeholder stub;
 * intentionally not a redirect to `/education`.
 */
export default function Landing() {
  return (
    <section aria-labelledby="landing-heading">
      <h1 id="landing-heading">Home</h1>
      <p>Landing page placeholder.</p>
    </section>
  );
}
