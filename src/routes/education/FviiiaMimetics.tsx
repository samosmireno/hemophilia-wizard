/**
 * `/education/fviiia-mimetics` — CONTEXT.md §7.5. A wizard cross-link target
 * (issue 08), so this slug is contractual.
 *
 * Placeholder until its design lands. Content is already modelled: the
 * `fviiia-mimetics`, `emicizumab-overview` and `denecimig-overview` topics.
 */
export default function FviiiaMimetics() {
  return (
    <section aria-labelledby="education-heading">
      <h1 id="education-heading">Education — fviiia-mimetics</h1>
      <p>Education chapter placeholder.</p>
    </section>
  );
}
