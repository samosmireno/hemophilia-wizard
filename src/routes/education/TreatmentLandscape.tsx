/**
 * `/education/treatment-landscape` — CONTEXT.md §7.1, §7.3, §7.4.
 *
 * Placeholder until its design lands. Content is already modelled:
 * `TREATMENT_OPTIONS_MATRIX` + `TREATMENT_OPTIONS_FOOTNOTES` (§7.3) and the
 * `clotting-factor-replacement` topic's benefits/challenges pair (§7.4).
 */
export default function TreatmentLandscape() {
  return (
    <section aria-labelledby="education-heading">
      <h1 id="education-heading">Education — treatment-landscape</h1>
      <p>Education chapter placeholder.</p>
    </section>
  );
}
