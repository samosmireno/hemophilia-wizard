/**
 * `/education/rebalancing-agents` — CONTEXT.md §7.6. A wizard cross-link target
 * (issue 08), so this slug is contractual.
 *
 * Placeholder until its design lands. Content is already modelled: the
 * `rebalancing-agents`, `nft` and `emerging-mimetics` topics.
 */
export default function RebalancingAgents() {
  return (
    <section aria-labelledby="education-heading">
      <h1 id="education-heading">Education — rebalancing-agents</h1>
      <p>Education chapter placeholder.</p>
    </section>
  );
}
