# 13 — Survey UI

Status: ready-for-human
Phase: 1
Blocked by: 06

## Goal

Build the post-use outcomes survey at `/survey`: three Likert/usage questions, validation,
submit through the adapter, confirmation state.

## Remaining

All of it. `Survey.tsx` is a placeholder, `SURVEY_QUESTIONS` is unconsumed, there is no
`LikertScale` primitive (issue 03), and there is no `submitSurvey` to call (issue 06).
